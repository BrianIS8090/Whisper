# 🛠️ Документация: Создание установщика WisperAI

Данная документация описывает процесс создания Windows-установщика для приложения WisperAI с использованием **PyInstaller** и **Inno Setup**.

---

## 📋 Оглавление

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Необходимые инструменты](#необходимые-инструменты)
3. [Структура файлов сборки](#структура-файлов-сборки)
4. [PyInstaller: Компиляция Python в EXE](#pyinstaller-компиляция-python-в-exe)
5. [Inno Setup: Создание установщика](#inno-setup-создание-установщика)
6. [Автоматизация сборки](#автоматизация-сборки)
7. [Решение проблем](#решение-проблем)

---

## Обзор архитектуры

### Почему не один большой EXE?

| Подход | Плюсы | Минусы |
|--------|-------|--------|
| **Onefile** (один EXE) | Один файл для распространения | Медленный запуск (распаковка во temp), большой размер |
| **Onedir + Installer** | Быстрый запуск, меньше размер после сжатия | Нужен установщик |

**Выбрано:** Сборка в директорию (`onedir`) + установщик Inno Setup.

### Схема процесса сборки

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Python код    │ ──► │   PyInstaller   │ ──► │  dist/WisperAI/ │
│   + ресурсы     │     │   (spec файл)   │     │  (exe + dll)    │
└─────────────────┘     └─────────────────┘     └────────┬────────┘
                                                         │
                                                         ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  WisperAI_Setup │ ◄── │   Inno Setup    │ ◄── │  dist/WisperAI/ │
│      .exe       │     │   (iss файл)    │     │  + FFmpeg       │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

---

## Необходимые инструменты

### 1. PyInstaller
Компилирует Python-приложение в standalone EXE.

```bash
pip install pyinstaller
```

### 2. Inno Setup 6
Создаёт Windows-установщик (.exe) с UI, ярлыками и деинсталлятором.

- **Скачать:** https://jrsoftware.org/isdl.php
- **Установить** в стандартную папку `C:\Program Files (x86)\Inno Setup 6\`

### 3. FFmpeg
Требуется для библиотеки Whisper (декодирование аудио).

```bash
winget install Gyan.FFmpeg
```

---

## Структура файлов сборки

```
Frontend/
├── main.py                      # Точка входа приложения
├── workers.py                   # Логика распознавания
├── assets/
│   └── asteroid.ico             # Иконка приложения
├── WisperAI_installer.spec      # Конфигурация PyInstaller
├── build_installer.bat          # Скрипт автоматической сборки
├── installer/
│   └── WisperAI_setup.iss       # Скрипт Inno Setup
├── build/                       # Временные файлы сборки (генерируется)
└── dist/
    ├── WisperAI/                # Скомпилированное приложение
    │   ├── WisperAI.exe
    │   ├── ffmpeg.exe
    │   ├── ffprobe.exe
    │   └── _internal/           # Python runtime + библиотеки
    └── WisperAI_Setup.exe       # Готовый установщик
```

---

## PyInstaller: Компиляция Python в EXE

### Файл: `WisperAI_installer.spec`

Spec-файл — это Python-скрипт конфигурации PyInstaller.

```python
# -*- mode: python ; coding: utf-8 -*-
import os
from PyInstaller.utils.hooks import collect_all

# ═══════════════════════════════════════════════════════════════════
# 1. ВНЕШНИЕ ЗАВИСИМОСТИ
# ═══════════════════════════════════════════════════════════════════

# Путь к FFmpeg (нужен для whisper)
ffmpeg_path = r"C:\Users\Brian\AppData\Local\Microsoft\WinGet\Packages\Gyan.FFmpeg_Microsoft.Winget.Source_8wekyb3d8bbwe\ffmpeg-8.0-full_build\bin"

# ═══════════════════════════════════════════════════════════════════
# 2. ДАННЫЕ И БИНАРНЫЕ ФАЙЛЫ
# ═══════════════════════════════════════════════════════════════════

# datas — файлы данных (копируются как есть)
# Формат: (источник, папка_назначения_относительно_exe)
datas = [
    ('assets', 'assets'),  # Иконки и ресурсы
]

# binaries — исполняемые файлы и DLL
# Формат: (путь_к_файлу, папка_назначения)
binaries = [
    (os.path.join(ffmpeg_path, 'ffmpeg.exe'), '.'),   # В корень
    (os.path.join(ffmpeg_path, 'ffprobe.exe'), '.'),
]

# ═══════════════════════════════════════════════════════════════════
# 3. СКРЫТЫЕ ИМПОРТЫ
# ═══════════════════════════════════════════════════════════════════

# Модули, которые PyInstaller не обнаруживает автоматически
hiddenimports = ['pyaudio', 'keyboard', 'groq', 'requests']

# ═══════════════════════════════════════════════════════════════════
# 4. СБОР ЗАВИСИМОСТЕЙ БИБЛИОТЕК
# ═══════════════════════════════════════════════════════════════════

# collect_all() собирает ВСЕ файлы библиотеки:
# - datas (данные)
# - binaries (dll)
# - hiddenimports (модули)

# QFluentWidgets — UI библиотека
tmp_ret = collect_all('qfluentwidgets')
datas += tmp_ret[0]
binaries += tmp_ret[1]
hiddenimports += tmp_ret[2]

# Whisper — распознавание речи
tmp_ret = collect_all('whisper')
datas += tmp_ret[0]
binaries += tmp_ret[1]
hiddenimports += tmp_ret[2]

# ═══════════════════════════════════════════════════════════════════
# 5. АНАЛИЗ
# ═══════════════════════════════════════════════════════════════════

a = Analysis(
    ['main.py'],              # Точка входа
    pathex=[],                # Дополнительные пути поиска
    binaries=binaries,
    datas=datas,
    hiddenimports=hiddenimports,
    hookspath=[],             # Кастомные хуки
    hooksconfig={},
    runtime_hooks=[],         # Скрипты, выполняемые ДО main.py
    excludes=[                # Исключить ненужные модули
        'PyQt5',              # Конфликтует с PyQt6
        'PyQt5.QtCore',
        'PyQt5.QtGui',
        'PyQt5.QtWidgets'
    ],
    noarchive=False,
    optimize=0,
)

# ═══════════════════════════════════════════════════════════════════
# 6. АРХИВ PYZ
# ═══════════════════════════════════════════════════════════════════

# PYZ — архив с скомпилированными .pyc модулями
pyz = PYZ(a.pure)

# ═══════════════════════════════════════════════════════════════════
# 7. ИСПОЛНЯЕМЫЙ ФАЙЛ
# ═══════════════════════════════════════════════════════════════════

exe = EXE(
    pyz,
    a.scripts,
    [],                       # ВАЖНО: пустой для onedir
    exclude_binaries=True,    # ВАЖНО: binaries в отдельных файлах
    name='WisperAI',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,                 # Сжатие UPX
    console=False,            # БЕЗ консольного окна (GUI)
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['assets\\asteroid.ico'],  # Иконка EXE
)

# ═══════════════════════════════════════════════════════════════════
# 8. СБОРКА В ДИРЕКТОРИЮ
# ═══════════════════════════════════════════════════════════════════

# COLLECT собирает все файлы в одну папку dist/WisperAI/
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='WisperAI',          # Имя выходной папки
)
```

### Ключевые моменты

1. **`exclude_binaries=True`** в EXE — обязательно для сборки в директорию
2. **`console=False`** — без консольного окна (GUI приложение)
3. **`collect_all()`** — собирает все файлы сложных библиотек (whisper, qfluentwidgets)
4. **`excludes`** — исключаем PyQt5, чтобы не конфликтовал с PyQt6

### Запуск PyInstaller

```bash
cd Frontend
..\venv\Scripts\pyinstaller --clean --noconfirm WisperAI_installer.spec
```

**Результат:** папка `dist/WisperAI/` с ~500+ файлами

---

## Inno Setup: Создание установщика

### Файл: `installer/WisperAI_setup.iss`

```iss
; ═══════════════════════════════════════════════════════════════════
; МЕТАДАННЫЕ ПРИЛОЖЕНИЯ
; ═══════════════════════════════════════════════════════════════════

#define MyAppName "Wisper AI"
#define MyAppVersion "1.0"
#define MyAppPublisher "Wisper"
#define MyAppExeName "WisperAI.exe"

; ═══════════════════════════════════════════════════════════════════
; НАСТРОЙКИ УСТАНОВЩИКА
; ═══════════════════════════════════════════════════════════════════

[Setup]
; Уникальный ID приложения (генерируйте свой!)
AppId={{A1B2C3D4-E5F6-7890-ABCD-EF1234567890}
AppName={#MyAppName}
AppVersion={#MyAppVersion}
AppPublisher={#MyAppPublisher}

; Папка установки по умолчанию
DefaultDirName={autopf}\{#MyAppName}
; {autopf} = C:\Program Files или C:\Program Files (x86)

; Группа в меню Пуск
DefaultGroupName={#MyAppName}

; Выходной файл
OutputDir=..\dist
OutputBaseFilename=WisperAI_Setup

; Иконка установщика
SetupIconFile=..\assets\asteroid.ico

; Сжатие (LZMA2 — лучшее сжатие)
Compression=lzma2/ultra64
SolidCompression=yes

; Современный интерфейс
WizardStyle=modern

; Требовать права администратора
PrivilegesRequired=admin

; Только 64-битная установка
ArchitecturesAllowed=x64compatible
ArchitecturesInstallIn64BitMode=x64compatible

; ═══════════════════════════════════════════════════════════════════
; ЯЗЫКИ
; ═══════════════════════════════════════════════════════════════════

[Languages]
Name: "russian"; MessagesFile: "compiler:Languages\Russian.isl"
Name: "english"; MessagesFile: "compiler:Default.isl"

; ═══════════════════════════════════════════════════════════════════
; ЗАДАЧИ (опциональные действия при установке)
; ═══════════════════════════════════════════════════════════════════

[Tasks]
Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; \
    GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked

; ═══════════════════════════════════════════════════════════════════
; ФАЙЛЫ ДЛЯ КОПИРОВАНИЯ
; ═══════════════════════════════════════════════════════════════════

[Files]
; Копируем ВСЕ файлы из dist\WisperAI рекурсивно
Source: "..\dist\WisperAI\*"; DestDir: "{app}"; \
    Flags: ignoreversion recursesubdirs createallsubdirs

; ═══════════════════════════════════════════════════════════════════
; ЯРЛЫКИ
; ═══════════════════════════════════════════════════════════════════

[Icons]
; Ярлык в меню Пуск
Name: "{group}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"

; Ярлык деинсталлятора
Name: "{group}\{cm:UninstallProgram,{#MyAppName}}"; Filename: "{uninstallexe}"

; Ярлык на рабочем столе (если выбрано)
Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; \
    Tasks: desktopicon

; ═══════════════════════════════════════════════════════════════════
; ДЕЙСТВИЯ ПОСЛЕ УСТАНОВКИ
; ═══════════════════════════════════════════════════════════════════

[Run]
; Запустить приложение после установки (опционально)
Filename: "{app}\{#MyAppExeName}"; \
    Description: "{cm:LaunchProgram,{#StringChange(MyAppName, '&', '&&')}}"; \
    Flags: nowait postinstall skipifsilent
```

### Ключевые секции

| Секция | Назначение |
|--------|------------|
| `[Setup]` | Основные настройки установщика |
| `[Languages]` | Поддерживаемые языки интерфейса |
| `[Tasks]` | Опции для пользователя (ярлык на рабочий стол) |
| `[Files]` | Какие файлы копировать и куда |
| `[Icons]` | Создание ярлыков |
| `[Run]` | Действия после установки |

### Запуск Inno Setup

```bash
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\WisperAI_setup.iss
```

**Результат:** файл `dist/WisperAI_Setup.exe`

---

## Автоматизация сборки

### Файл: `build_installer.bat`

```batch
@echo off
setlocal enabledelayedexpansion
cd /d "%~dp0"

echo ============================================
echo    WisperAI Installer Builder
echo ============================================
echo.

:: ═══════════════════════════════════════════════════════════════════
:: ШАГ 1: PyInstaller
:: ═══════════════════════════════════════════════════════════════════

echo [1/2] Сборка приложения через PyInstaller...

if exist "dist\WisperAI" (
    echo Удаление предыдущей сборки...
    rmdir /s /q "dist\WisperAI"
)

..\venv\Scripts\pyinstaller --clean --noconfirm WisperAI_installer.spec

if errorlevel 1 (
    echo ОШИБКА: PyInstaller завершился с ошибкой!
    pause
    exit /b 1
)

:: ═══════════════════════════════════════════════════════════════════
:: КОПИРОВАНИЕ FFMPEG В КОРЕНЬ
:: ═══════════════════════════════════════════════════════════════════

echo Копирование FFmpeg в корень сборки...
copy "dist\WisperAI\_internal\ffmpeg.exe" "dist\WisperAI\ffmpeg.exe" >nul 2>&1
copy "dist\WisperAI\_internal\ffprobe.exe" "dist\WisperAI\ffprobe.exe" >nul 2>&1

echo PyInstaller сборка завершена успешно!

:: ═══════════════════════════════════════════════════════════════════
:: ШАГ 2: Inno Setup
:: ═══════════════════════════════════════════════════════════════════

echo [2/2] Создание установщика через Inno Setup...

:: Поиск Inno Setup
set "ISCC="
if exist "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" (
    set "ISCC=C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
) else if exist "C:\Program Files\Inno Setup 6\ISCC.exe" (
    set "ISCC=C:\Program Files\Inno Setup 6\ISCC.exe"
)

if "!ISCC!"=="" (
    echo ВНИМАНИЕ: Inno Setup не найден!
    echo Скачайте: https://jrsoftware.org/isdl.php
    pause
    exit /b 0
)

"!ISCC!" "installer\WisperAI_setup.iss"

if errorlevel 1 (
    echo ОШИБКА: Inno Setup завершился с ошибкой!
    pause
    exit /b 1
)

echo ============================================
echo    СБОРКА ЗАВЕРШЕНА УСПЕШНО!
echo ============================================
echo Установщик: dist\WisperAI_Setup.exe
pause
```

### Использование

```bash
cd c:\Wisper\Frontend
build_installer.bat
```

---

## Решение проблем

### Проблема: Мелькает консольное окно при распознавании

**Причина:** Whisper вызывает FFmpeg через `subprocess.Popen()`, который на Windows показывает консоль.

**Решение:** Monkey-patch в `workers.py`:

```python
import subprocess
import sys

# Патч для скрытия консольного окна ffmpeg на Windows
if sys.platform == "win32":
    _original_popen = subprocess.Popen
    
    def _popen_no_console(*args, **kwargs):
        if 'creationflags' not in kwargs:
            kwargs['creationflags'] = subprocess.CREATE_NO_WINDOW
        return _original_popen(*args, **kwargs)
    
    subprocess.Popen = _popen_no_console

# Импортируем whisper ПОСЛЕ патча
import whisper
```

### Проблема: FFmpeg не найден на целевом ПК

**Причина:** FFmpeg не включён в сборку или лежит в `_internal/`.

**Решение:** 
1. Добавить в spec: `binaries = [(ffmpeg_path + '\\ffmpeg.exe', '.')]`
2. В `build_installer.bat` копировать в корень:
   ```batch
   copy "dist\WisperAI\_internal\ffmpeg.exe" "dist\WisperAI\ffmpeg.exe"
   ```

### Проблема: PyQt5 конфликтует с PyQt6

**Причина:** qfluentwidgets тянет оба.

**Решение:** Исключить в spec:
```python
excludes=['PyQt5', 'PyQt5.QtCore', 'PyQt5.QtGui', 'PyQt5.QtWidgets'],
```

### Проблема: Модуль не найден (ModuleNotFoundError)

**Решение:** Добавить в `hiddenimports`:
```python
hiddenimports = ['pyaudio', 'keyboard', 'groq', 'requests', 'ваш_модуль']
```

---

## 📦 Итоговая структура установленного приложения

```
C:\Program Files\Wisper AI\
├── WisperAI.exe          # Главный исполняемый файл
├── ffmpeg.exe            # FFmpeg для whisper
├── ffprobe.exe           # FFprobe для whisper
├── _internal/            # Python runtime + библиотеки
│   ├── python313.dll
│   ├── torch/
│   ├── whisper/
│   ├── PyQt6/
│   ├── qfluentwidgets/
│   └── ...
└── assets/
    └── asteroid.ico
```

**Размер:** ~350 MB (с FFmpeg)

---

## 🔗 Полезные ссылки

- [PyInstaller документация](https://pyinstaller.org/en/stable/)
- [Inno Setup документация](https://jrsoftware.org/ishelp/)
- [FFmpeg загрузка](https://www.gyan.dev/ffmpeg/builds/)
