# 🛠️ Создание Windows-установщика для Python-приложений

Универсальное руководство по упаковке Python-приложений в standalone-установщик с использованием **PyInstaller** и **Inno Setup**.

---

## 📋 Оглавление

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Необходимые инструменты](#необходимые-инструменты)
3. [Структура проекта](#структура-проекта)
4. [PyInstaller: Компиляция Python в EXE](#pyinstaller-компиляция-python-в-exe)
5. [Inno Setup: Создание установщика](#inno-setup-создание-установщика)
6. [Автоматизация сборки](#автоматизация-сборки)
7. [Решение типичных проблем](#решение-типичных-проблем)

---

## Обзор архитектуры

### Цель

Создать Windows-установщик (.exe), который:
- Устанавливает приложение в `Program Files`
- Создаёт ярлыки на рабочем столе и в меню Пуск
- Включает деинсталлятор
- **Работает без установленного Python** на целевой машине

### Двухэтапный процесс

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Python код    │ ──► │   PyInstaller   │ ──► │   dist/MyApp/   │
│   + ресурсы     │     │   (spec файл)   │     │   (exe + dll)   │
└─────────────────┘     └─────────────────┘     └────────┬────────┘
                                                         │
                                                         ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  MyApp_Setup    │ ◄── │   Inno Setup    │ ◄── │   dist/MyApp/   │
│      .exe       │     │   (iss файл)    │     │   + доп.файлы   │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

### Onefile vs Onedir

| Критерий | onefile | onedir |
|----------|---------|--------|
| Время запуска | Медленно (распаковка) | Быстро |
| Размер установщика | Больше | Меньше (лучше сжатие) |
| Обновление | Заменить весь файл | Можно патчить отдельные файлы |
| Антивирусы | Часто ложные срабатывания | Меньше проблем |
| Отладка | Сложно | Легко |

**Рекомендация:** Для приложений > 50 МБ используйте `onedir` + установщик.

---

## Необходимые инструменты

### 1. PyInstaller

Компилирует Python-приложение в standalone EXE со всеми зависимостями.

```bash
pip install pyinstaller
```

### 2. Inno Setup 6

Создаёт Windows-установщик с красивым UI, ярлыками и деинсталлятором.

- **Скачать:** https://jrsoftware.org/isdl.php
- **Установить** в `C:\Program Files (x86)\Inno Setup 6\`

### 3. Внешние зависимости (опционально)

Если ваше приложение использует внешние программы (FFmpeg, ImageMagick и т.д.), их нужно включить в сборку.

---

## Структура проекта

```
MyProject/
├── src/
│   ├── main.py                  # Точка входа приложения
│   ├── module1.py               # Ваши модули
│   └── assets/
│       └── icon.ico             # Иконка приложения
├── MyApp.spec                   # Конфигурация PyInstaller
├── build_installer.bat          # Скрипт автоматической сборки
├── installer/
│   └── setup.iss                # Скрипт Inno Setup
├── build/                       # Временные файлы (генерируется)
└── dist/
    ├── MyApp/                   # Скомпилированное приложение
    │   ├── MyApp.exe
    │   └── _internal/           # Python runtime + библиотеки
    └── MyApp_Setup.exe          # Готовый установщик
```

---

## PyInstaller: Компиляция Python в EXE

### Базовый spec-файл

Создайте файл `MyApp.spec`:

```python
# -*- mode: python ; coding: utf-8 -*-
from PyInstaller.utils.hooks import collect_all

# ═══════════════════════════════════════════════════════════════════
# 1. ДАННЫЕ И РЕСУРСЫ
# ═══════════════════════════════════════════════════════════════════

# Формат: (путь_источника, папка_назначения_относительно_exe)
datas = [
    ('src/assets', 'assets'),      # Иконки, изображения
    ('config.json', '.'),          # Конфиги в корень
]

# Бинарные файлы (dll, exe)
binaries = [
    # ('path/to/external.exe', '.'),  # Внешние программы
]

# ═══════════════════════════════════════════════════════════════════
# 2. СКРЫТЫЕ ИМПОРТЫ
# ═══════════════════════════════════════════════════════════════════

# Модули, которые PyInstaller не обнаруживает автоматически
hiddenimports = [
    # 'some_module',
    # 'another_module',
]

# ═══════════════════════════════════════════════════════════════════
# 3. СБОР СЛОЖНЫХ БИБЛИОТЕК
# ═══════════════════════════════════════════════════════════════════

# Для библиотек с множеством файлов данных используйте collect_all()
# Пример для PyQt/PySide:
# tmp = collect_all('PyQt6')
# datas += tmp[0]; binaries += tmp[1]; hiddenimports += tmp[2]

# ═══════════════════════════════════════════════════════════════════
# 4. АНАЛИЗ
# ═══════════════════════════════════════════════════════════════════

a = Analysis(
    ['src/main.py'],              # Точка входа
    pathex=[],                    # Дополнительные пути поиска модулей
    binaries=binaries,
    datas=datas,
    hiddenimports=hiddenimports,
    hookspath=[],                 # Папка с кастомными хуками
    hooksconfig={},
    runtime_hooks=[],             # Скрипты, выполняемые ДО main.py
    excludes=[
        # Исключить ненужные/конфликтующие модули
        # 'tkinter',
        # 'PyQt5',
    ],
    noarchive=False,
    optimize=0,
)

# ═══════════════════════════════════════════════════════════════════
# 5. АРХИВ PYZ
# ═══════════════════════════════════════════════════════════════════

pyz = PYZ(a.pure)

# ═══════════════════════════════════════════════════════════════════
# 6. ИСПОЛНЯЕМЫЙ ФАЙЛ
# ═══════════════════════════════════════════════════════════════════

exe = EXE(
    pyz,
    a.scripts,
    [],                           # ВАЖНО: пустой список для onedir
    exclude_binaries=True,        # ВАЖНО: True для onedir
    name='MyApp',                 # Имя exe-файла
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,                     # Сжатие UPX (ускоряет загрузку)
    console=False,                # False для GUI, True для консольных
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['src/assets/icon.ico'], # Иконка EXE
)

# ═══════════════════════════════════════════════════════════════════
# 7. СБОРКА В ДИРЕКТОРИЮ
# ═══════════════════════════════════════════════════════════════════

coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='MyApp',                 # Имя выходной папки в dist/
)
```

### Ключевые параметры

| Параметр | Описание |
|----------|----------|
| `exclude_binaries=True` | Обязательно для onedir-сборки |
| `console=False` | Убирает консольное окно (для GUI) |
| `console=True` | Показывает консоль (для CLI) |
| `upx=True` | Сжимает бинарники (уменьшает размер) |
| `icon` | Иконка исполняемого файла |

### Запуск PyInstaller

```bash
pyinstaller --clean --noconfirm MyApp.spec
```

**Результат:** папка `dist/MyApp/` со всеми файлами

---

## Inno Setup: Создание установщика

### Базовый iss-файл

Создайте файл `installer/setup.iss`:

```iss
; ═══════════════════════════════════════════════════════════════════
; МЕТАДАННЫЕ ПРИЛОЖЕНИЯ
; ═══════════════════════════════════════════════════════════════════

; Замените на свои значения
#define MyAppName "My Application"
#define MyAppVersion "1.0.0"
#define MyAppPublisher "Your Company"
#define MyAppURL "https://example.com"
#define MyAppExeName "MyApp.exe"

; ═══════════════════════════════════════════════════════════════════
; НАСТРОЙКИ УСТАНОВЩИКА
; ═══════════════════════════════════════════════════════════════════

[Setup]
; ВАЖНО: Сгенерируйте уникальный GUID для вашего приложения
; Используйте: https://www.guidgenerator.com/
AppId={{XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX}
AppName={#MyAppName}
AppVersion={#MyAppVersion}
AppPublisher={#MyAppPublisher}
AppPublisherURL={#MyAppURL}
AppSupportURL={#MyAppURL}
AppUpdatesURL={#MyAppURL}

; Папка установки по умолчанию
DefaultDirName={autopf}\{#MyAppName}
; {autopf} = C:\Program Files (64-bit) или C:\Program Files (x86) (32-bit)

; Группа в меню Пуск
DefaultGroupName={#MyAppName}

; Разрешить пользователю изменять папку меню Пуск
AllowNoIcons=yes

; Куда сохранять готовый установщик
OutputDir=..\dist
OutputBaseFilename={#MyAppName}_Setup

; Иконка установщика (опционально)
SetupIconFile=..\src\assets\icon.ico

; Алгоритм сжатия (lzma2 — лучшее сжатие)
Compression=lzma2/ultra64
SolidCompression=yes

; Современный стиль мастера установки
WizardStyle=modern

; Требовать права администратора (для Program Files)
PrivilegesRequired=admin

; Только 64-битная Windows
ArchitecturesAllowed=x64compatible
ArchitecturesInstallIn64BitMode=x64compatible

; Показать лицензию (опционально)
; LicenseFile=..\LICENSE.txt

; ═══════════════════════════════════════════════════════════════════
; ЯЗЫКИ ИНТЕРФЕЙСА
; ═══════════════════════════════════════════════════════════════════

[Languages]
Name: "english"; MessagesFile: "compiler:Default.isl"
Name: "russian"; MessagesFile: "compiler:Languages\Russian.isl"

; ═══════════════════════════════════════════════════════════════════
; ОПЦИИ УСТАНОВКИ
; ═══════════════════════════════════════════════════════════════════

[Tasks]
; Ярлык на рабочем столе (по умолчанию не выбран)
Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; \
    GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked

; ═══════════════════════════════════════════════════════════════════
; ФАЙЛЫ ДЛЯ УСТАНОВКИ
; ═══════════════════════════════════════════════════════════════════

[Files]
; Копируем ВСЕ файлы из папки сборки рекурсивно
Source: "..\dist\MyApp\*"; DestDir: "{app}"; \
    Flags: ignoreversion recursesubdirs createallsubdirs

; ═══════════════════════════════════════════════════════════════════
; ЯРЛЫКИ
; ═══════════════════════════════════════════════════════════════════

[Icons]
; Ярлык в меню Пуск
Name: "{group}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"

; Ярлык деинсталлятора в меню Пуск
Name: "{group}\Удалить {#MyAppName}"; Filename: "{uninstallexe}"

; Ярлык на рабочем столе (если пользователь выбрал)
Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; \
    Tasks: desktopicon

; ═══════════════════════════════════════════════════════════════════
; ДЕЙСТВИЯ ПОСЛЕ УСТАНОВКИ
; ═══════════════════════════════════════════════════════════════════

[Run]
; Запустить приложение после установки (опционально)
Filename: "{app}\{#MyAppExeName}"; \
    Description: "Запустить {#MyAppName}"; \
    Flags: nowait postinstall skipifsilent
```

### Переменные Inno Setup

| Переменная | Значение |
|------------|----------|
| `{app}` | Папка установки (выбирает пользователь) |
| `{autopf}` | Program Files (автоопределение 32/64 bit) |
| `{autodesktop}` | Рабочий стол текущего пользователя |
| `{group}` | Папка в меню Пуск |
| `{uninstallexe}` | Путь к деинсталлятору |

### Запуск Inno Setup

```bash
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\setup.iss
```

**Результат:** файл `dist/MyApp_Setup.exe`

---

## Автоматизация сборки

### Файл: `build_installer.bat`

```batch
@echo off
setlocal enabledelayedexpansion
cd /d "%~dp0"

echo ============================================
echo    Сборка установщика
echo ============================================
echo.

:: ═══════════════════════════════════════════════════════════════════
:: ШАГ 1: PyInstaller
:: ═══════════════════════════════════════════════════════════════════

echo [1/2] Сборка приложения через PyInstaller...

:: Удаляем предыдущую сборку
if exist "dist\MyApp" (
    echo Удаление предыдущей сборки...
    rmdir /s /q "dist\MyApp"
)

:: Запускаем PyInstaller
:: Если используете venv:
:: ..\venv\Scripts\pyinstaller --clean --noconfirm MyApp.spec
:: Если глобально:
pyinstaller --clean --noconfirm MyApp.spec

if errorlevel 1 (
    echo ОШИБКА: PyInstaller завершился с ошибкой!
    pause
    exit /b 1
)

echo PyInstaller: OK

:: ═══════════════════════════════════════════════════════════════════
:: ДОПОЛНИТЕЛЬНЫЕ ФАЙЛЫ (опционально)
:: ═══════════════════════════════════════════════════════════════════

:: Пример: копирование внешних программ в корень сборки
:: copy "path\to\ffmpeg.exe" "dist\MyApp\ffmpeg.exe" >nul 2>&1

:: ═══════════════════════════════════════════════════════════════════
:: ШАГ 2: Inno Setup
:: ═══════════════════════════════════════════════════════════════════

echo [2/2] Создание установщика через Inno Setup...

:: Поиск Inno Setup
set "ISCC="
if exist "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" (
    set "ISCC=C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
) else if exist "C:\Program Files\Inno Setup 6\ISCC.exe" (
    set "ISCC=C:\Program Files\Inno Setup 6\ISCC.exe"
)

if "!ISCC!"=="" (
    echo ВНИМАНИЕ: Inno Setup не найден!
    echo Скачайте: https://jrsoftware.org/isdl.php
    pause
    exit /b 1
)

"!ISCC!" "installer\setup.iss"

if errorlevel 1 (
    echo ОШИБКА: Inno Setup завершился с ошибкой!
    pause
    exit /b 1
)

echo.
echo ============================================
echo    СБОРКА ЗАВЕРШЕНА УСПЕШНО!
echo ============================================
echo Установщик: dist\MyApp_Setup.exe
echo.
pause
```

---

## Решение типичных проблем

### Проблема: ModuleNotFoundError

**Причина:** PyInstaller не обнаружил динамический импорт.

**Решение:** Добавьте в spec-файл:
```python
hiddenimports = ['missing_module', 'another_module']
```

### Проблема: Файлы данных не найдены

**Причина:** Пути к файлам жёстко заданы в коде.

**Решение:** Используйте динамическое определение пути:
```python
import sys
import os

if getattr(sys, 'frozen', False):
    # Запуск как EXE
    base_path = sys._MEIPASS  # Для onefile
    # или
    base_path = os.path.dirname(sys.executable)  # Для onedir
else:
    # Запуск как скрипт
    base_path = os.path.dirname(os.path.abspath(__file__))

config_path = os.path.join(base_path, 'config.json')
```

### Проблема: Мелькает консольное окно

**Причина:** Вызов `subprocess.Popen()` без флага скрытия окна.

**Решение:** Добавьте в начало программы:
```python
import subprocess
import sys

if sys.platform == "win32":
    _original_popen = subprocess.Popen
    
    def _popen_no_console(*args, **kwargs):
        if 'creationflags' not in kwargs:
            kwargs['creationflags'] = subprocess.CREATE_NO_WINDOW
        return _original_popen(*args, **kwargs)
    
    subprocess.Popen = _popen_no_console
```

### Проблема: Permission denied при записи файлов

**Причина:** Попытка записи в `Program Files` (требует прав администратора).

**Решение:** Храните пользовательские данные в `%APPDATA%`:
```python
import os

if getattr(sys, 'frozen', False):
    # Для установленного приложения
    data_dir = os.path.join(os.environ.get('APPDATA', ''), 'MyApp')
    os.makedirs(data_dir, exist_ok=True)
    config_path = os.path.join(data_dir, 'config.json')
else:
    # Для разработки
    config_path = 'config.json'
```

### Проблема: Конфликт версий библиотек

**Пример:** PyQt5 конфликтует с PyQt6.

**Решение:** Исключите ненужную версию:
```python
excludes = ['PyQt5', 'PyQt5.QtCore', 'PyQt5.QtGui', 'PyQt5.QtWidgets']
```

### Проблема: Антивирус блокирует EXE

**Причина:** Эвристический анализ ложно срабатывает на упакованные файлы.

**Решение:**
1. Используйте `onedir` вместо `onefile`
2. Подпишите EXE сертификатом (code signing)
3. Добавьте исключение в антивирус

---

## 📦 Итоговая структура

После установки на целевом ПК:

```
C:\Program Files\My Application\
├── MyApp.exe             # Главный исполняемый файл
├── _internal/            # Python runtime + библиотеки
│   ├── python3XX.dll     # Интерпретатор Python
│   ├── library.zip       # Скомпилированные модули
│   ├── PyQt6/            # GUI (если используется)
│   └── ...               # Остальные зависимости
└── assets/               # Ресурсы приложения
    └── icon.ico
```

Пользовательские данные (конфиги, кэш):
```
C:\Users\{username}\AppData\Roaming\MyApp\
├── config.json
└── cache/
```

---

## 🔗 Полезные ссылки

- [PyInstaller документация](https://pyinstaller.org/en/stable/)
- [PyInstaller hooks](https://pyinstaller.org/en/stable/hooks.html)
- [Inno Setup документация](https://jrsoftware.org/ishelp/)
- [Inno Setup примеры](https://jrsoftware.org/isinfo.php)
- [UPX (сжатие)](https://upx.github.io/)
